import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks"
import * as AgentCardStories from "./AgentCard.stories"

<Meta of={AgentCardStories} />

# AgentCard

A card component for the agent marketplace grid. Displays an agent's avatar, name, description, and category badges. The entire card is a link to the agent detail page.

## Import

```tsx
import { AgentCard } from "@/components/AgentCard"
```

## Usage

```tsx
<AgentCard
  slug="acme/customer-insight-engine"
  account="acme"
  name="Customer Insight Engine"
  description="Analyzes customer feedback to surface actionable insights and trends."
  categories={["Analytics"]}
/>
```

## Default

<Canvas of={AgentCardStories.Default} />

## Long Description

Descriptions are clamped to 2 lines.

<Canvas of={AgentCardStories.LongDescription} />

## Few Categories

<Canvas of={AgentCardStories.FewCategories} />

## Many Categories

Only the first 2 categories are shown.

<Canvas of={AgentCardStories.ManyCategories} />

## Props

<Controls of={AgentCardStories.Default} />
