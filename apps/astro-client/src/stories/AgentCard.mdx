import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks"
import * as AgentCardStories from "./AgentCard.stories"

<Meta of={AgentCardStories} />

# AgentCard

A card component for agent discovery surfaces. The default variant is used in marketplace/grid contexts, and the `oftenUsedTogether` variant is used in the sidebar recommendation stack on agent detail pages.

## Import

```tsx
import { AgentCard } from "@/components/AgentCard"
```

## Usage

```tsx
<AgentCard
  slug="acme/customer-insight-engine"
  account="acme"
  name="Customer Insight Engine"
  description="Analyzes customer feedback to surface actionable insights and trends."
/>
```

### Often Used Together Variant

```tsx
<AgentCard
  slug="steve_jobs/alert-router"
  account="steve_jobs"
  name="alert-router"
  description="Routes alerts to the correct responders."
  variant="oftenUsedTogether"
  rating={4.6}
  lifetimeMessages={1203}
/>
```

## Default

<Canvas of={AgentCardStories.Default} />

## Long Description

Descriptions are clamped to 3 lines in the default variant.

<Canvas of={AgentCardStories.LongDescription} />

## Short Description

<Canvas of={AgentCardStories.ShortDescription} />

## Different Account

<Canvas of={AgentCardStories.DifferentAccount} />

## Often Used Together

Compact recommendation card variant with contrast background and metadata line.

<Canvas of={AgentCardStories.OftenUsedTogether} />

## Grid

Cards laid out in a responsive grid.

<Canvas of={AgentCardStories.Grid} />

## Props

<Controls of={AgentCardStories.Default} />
