# Backend API URL - Vite proxy forwards /api and /auth requests to this target
# Default: http://localhost:8080 (local development)
# Set to your deployed server URL to develop against a remote backend
VITE_API_URL=http://localhost:8080
