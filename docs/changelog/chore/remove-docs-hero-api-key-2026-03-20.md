# Remove "Get API key" button from docs hero

## Summary

Removes the secondary CTA button from the docs homepage hero. The button linked to the settings page for API key generation but added noise to the hero — the Quickstart CTA is sufficient for new users landing on the docs.

## Design

The hero button group now renders a single primary button ("Quickstart") instead of two buttons.

## Migration

No action required.
