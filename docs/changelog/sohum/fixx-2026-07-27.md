# Keep one clear action in agent search

## Summary

Agent search now shows only the product-styled clear action instead of duplicating it with a browser-provided control.

## Design

The selector keeps its semantic search input and accessible clear button. A scoped WebKit appearance override suppresses the native cancel control in browsers that add one, without changing filtering, focus, or keyboard behavior.

## Migration

None.
