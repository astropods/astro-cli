## Summary

Renames "Install" to "Deploy" across the agent deployment flow for consistent terminology.

## Design

- CTA button on agent detail sidebar: "Install this agent" → "Deploy this agent"
- `InstallAgent` page title and header: "Install {account}/{name}" → "Deploy {account}/{name}"
- Deploy form general section description: "where to install it" → "where to deploy it"
- Deploy form account picker label: "Install to" → "Deploy to"
- Deploy form submit button: "Launch Agent" → "Deploy"

## Migration

No changes required.
