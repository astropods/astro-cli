# Filter Agents Input Update

## Summary

Refreshed the `FilterInput` component to better match the intended search bar design.

## Design

- Replaced the `Filter` icon (lucide-react) with `MagnifyingGlassIcon` from `@heroicons/react/24/outline`, consistent with the Heroicons icon system used across the app.
- Reduced height from `h-11` (44px) to `h-9` (36px) for a more compact feel.
- Standardized the placeholder text to `"Filter agents..."` in `MyAgentsHeader`.

## Migration

No migration required. The component API is unchanged.
