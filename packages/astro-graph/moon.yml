language: "typescript"
type: "library"

dependsOn:
  - "astro-types"
  - "astro-nodes"

tasks:
  build:
    command: "bun run tsc"
    inputs:
      - "src/**/*"
      - "tsconfig.json"
      - "/tsconfig.base.json"
    outputs:
      - "dist"
    deps:
      - "^:build"
