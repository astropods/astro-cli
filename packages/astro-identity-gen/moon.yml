language: "typescript"
type: "library"

tasks:
  build:
    command: "bun run tsc"
    deps:
      - "astro-theme:build"
    inputs:
      - "src/**/*"
      - "tsconfig.json"
      - "/tsconfig.base.json"
    outputs:
      - "dist"
