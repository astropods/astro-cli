# {{.Name}}

{{.Description}}

## Quick start

```bash
# Install dependencies
pip install -r requirements.txt

# Start the agent locally
ast dev
```

## Project structure

```
{{.Name}}/
├── agent/
│   └── main.py           # Agent entry point
{{- if gt (len .Ingestions) 0}}
├── ingestion/
{{- range .Ingestions}}
│   ├── {{.}}/
│   │   ├── main.py           # {{.}} ingestion pipeline
│   │   ├── requirements.txt  # {{.}} dependencies
│   │   └── Dockerfile
{{- end}}
{{- end}}
├── astropods.yml         # Agent specification
├── Dockerfile            # Agent container
├── requirements.txt      # Python dependencies
└── .env                  # Environment variables (set via ast configure; not committed)
```

## Configuration

The agent is configured in `astropods.yml`. Key sections:
{{- if ne .ModelProvider ""}}

### Model

Self-hosted **{{.ModelProvider}}** provider{{if ne .Model ""}} running `{{.Model}}`{{end}}.
{{- end}}
{{- if gt (len .Integrations) 0}}

### Integrations

| Integration | Type | Environment variable |
|------------|------|---------------------|
{{- range .Integrations}}
{{- if eq . "anthropic"}}
| Anthropic | Model API | `ANTHROPIC_API_KEY` |
{{- else if eq . "openai"}}
| OpenAI | Model API | `OPENAI_API_KEY` |
{{- else if eq . "github"}}
| GitHub | Tool | `GITHUB_TOKEN` |
{{- end}}
{{- end}}
{{- end}}
{{- if gt (len .Knowledge) 0}}

### Knowledge stores

{{- range .Knowledge}}
{{- if eq . "qdrant"}}
- **Qdrant** — vector store for semantic search and embeddings
{{- else if eq . "redis"}}
- **Redis** — key-value store for caching and fast lookups
{{- else if eq . "neo4j"}}
- **Neo4j** — graph database for relationship data
{{- end}}
{{- end}}
{{- end}}
{{- if gt (len .Interfaces) 0}}

### Interfaces

{{- range .Interfaces}}
{{- if eq . "web"}}
- **Web** — HTTP/SSE endpoint (playground available at `localhost:3100` during dev)
{{- else if eq . "slack"}}
- **Slack** — bot integration via Socket Mode
{{- end}}
{{- end}}
{{- end}}
{{- if gt (len .Ingestions) 0}}

### Ingestion

Data pipeline triggered via **{{range $i, $v := .Ingestions}}{{if $i}}, {{end}}{{$v}}{{end}}**. Edit `ingestion/<type>/main.py` to define how data flows into your knowledge stores.
{{- end}}


