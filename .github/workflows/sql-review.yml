name: SQL Review

on:
  pull_request:
    paths:
      - "sql/astro-server/schema.sql"
      - "sql/river/**"

jobs:
  schema-plan-preview:
    runs-on: ubuntu-latest
    environment: preview
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Atlas
        run: curl -sSf https://atlasgo.sh | sh

      - name: Generate declarative schema plan
        id: plan
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          OUTPUT=$(atlas schema diff \
            --from "$DATABASE_URL" \
            --to "file://sql/astro-server/schema.sql" \
            --dev-url "docker://postgres/17/dev" \
            --exclude "atlas_schema_revisions,river" 2>&1) && STATUS=0 || STATUS=$?
          if [ $STATUS -ne 0 ]; then
            echo "has_changes=error" >> "$GITHUB_OUTPUT"
          elif [ -z "$OUTPUT" ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi
          {
            echo "diff<<EOF"
            echo "$OUTPUT"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Check pending River migrations
        id: river
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          OUTPUT=$(atlas migrate status \
            --url "$DATABASE_URL" \
            --dir "file://sql/river" \
            --revisions-schema atlas_schema_revisions 2>&1) && STATUS=0 || STATUS=$?
          PENDING=$(echo "$OUTPUT" | grep -c "^  --" || true)
          if [ $STATUS -ne 0 ]; then
            echo "status=error" >> "$GITHUB_OUTPUT"
          elif [ "$PENDING" -gt 0 ]; then
            echo "status=pending" >> "$GITHUB_OUTPUT"
          else
            echo "status=current" >> "$GITHUB_OUTPUT"
          fi
          {
            echo "output<<EOF"
            echo "$OUTPUT"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const marker = '<!-- atlas-schema-plan-preview -->';
            const hasChanges = '${{ steps.plan.outputs.has_changes }}';
            const diff = `${{ steps.plan.outputs.diff }}`;
            const riverStatus = '${{ steps.river.outputs.status }}';
            const riverOutput = `${{ steps.river.outputs.output }}`;

            const sections = [];

            if (hasChanges === 'error') {
              sections.push(
                '### Declarative Schema',
                '',
                '> [!CAUTION]',
                '> Atlas failed to generate a migration plan.',
                '',
                '<details>',
                '<summary>Error output</summary>',
                '',
                '```',
                diff,
                '```',
                '',
                '</details>',
              );
            } else if (hasChanges === 'false') {
              sections.push(
                '### Declarative Schema',
                '',
                '> [!NOTE]',
                '> No schema changes detected. The preview database already matches `schema.sql`.',
              );
            } else {
              const lines = diff.split('\n');
              const adds = lines.filter(l => l.startsWith('+')).length;
              const drops = lines.filter(l => l.startsWith('-')).length;
              const alters = lines.filter(l => /^(ALTER|MODIFY)/i.test(l.trim())).length;

              const stats = [
                adds > 0 ? `**${adds}** addition${adds > 1 ? 's' : ''}` : '',
                drops > 0 ? `**${drops}** removal${drops > 1 ? 's' : ''}` : '',
                alters > 0 ? `**${alters}** alteration${alters > 1 ? 's' : ''}` : '',
              ].filter(Boolean).join(', ');

              sections.push(
                '### Declarative Schema',
                '',
                '> [!IMPORTANT]',
                '> The following SQL will be applied to the **preview** database on merge.',
                '',
                stats ? `${stats}\n` : '',
                '```sql',
                diff,
                '```',
              );
            }

            sections.push('', '### River Migrations', '');

            if (riverStatus === 'error') {
              sections.push(
                '> [!CAUTION]',
                '> Failed to check River migration status.',
                '',
                '<details>',
                '<summary>Error output</summary>',
                '',
                '```',
                riverOutput,
                '```',
                '',
                '</details>',
              );
            } else if (riverStatus === 'pending') {
              sections.push(
                '> [!IMPORTANT]',
                '> Pending River migrations will be applied on merge.',
                '',
                '```',
                riverOutput,
                '```',
              );
            } else {
              sections.push(
                '> [!NOTE]',
                '> All River migrations are up to date.',
              );
            }

            const body = [
              marker,
              '## Migration Plan — Preview',
              '',
              ...sections,
              '',
              `<sub>Generated by Atlas \u00b7 ${new Date().toISOString().slice(0, 10)}</sub>`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c => c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  schema-plan-production:
    runs-on: ubuntu-latest
    environment: prod
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Atlas
        run: curl -sSf https://atlasgo.sh | sh

      - name: Generate declarative schema plan
        id: plan
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          OUTPUT=$(atlas schema diff \
            --from "$DATABASE_URL" \
            --to "file://sql/astro-server/schema.sql" \
            --dev-url "docker://postgres/17/dev" \
            --exclude "atlas_schema_revisions,river" 2>&1) && STATUS=0 || STATUS=$?
          if [ $STATUS -ne 0 ]; then
            echo "has_changes=error" >> "$GITHUB_OUTPUT"
          elif [ -z "$OUTPUT" ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi
          {
            echo "diff<<EOF"
            echo "$OUTPUT"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Check pending River migrations
        id: river
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          OUTPUT=$(atlas migrate status \
            --url "$DATABASE_URL" \
            --dir "file://sql/river" \
            --revisions-schema atlas_schema_revisions 2>&1) && STATUS=0 || STATUS=$?
          PENDING=$(echo "$OUTPUT" | grep -c "^  --" || true)
          if [ $STATUS -ne 0 ]; then
            echo "status=error" >> "$GITHUB_OUTPUT"
          elif [ "$PENDING" -gt 0 ]; then
            echo "status=pending" >> "$GITHUB_OUTPUT"
          else
            echo "status=current" >> "$GITHUB_OUTPUT"
          fi
          {
            echo "output<<EOF"
            echo "$OUTPUT"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const marker = '<!-- atlas-schema-plan-production -->';
            const hasChanges = '${{ steps.plan.outputs.has_changes }}';
            const diff = `${{ steps.plan.outputs.diff }}`;
            const riverStatus = '${{ steps.river.outputs.status }}';
            const riverOutput = `${{ steps.river.outputs.output }}`;

            const sections = [];

            if (hasChanges === 'error') {
              sections.push(
                '### Declarative Schema',
                '',
                '> [!CAUTION]',
                '> Atlas failed to generate a migration plan.',
                '',
                '<details>',
                '<summary>Error output</summary>',
                '',
                '```',
                diff,
                '```',
                '',
                '</details>',
              );
            } else if (hasChanges === 'false') {
              sections.push(
                '### Declarative Schema',
                '',
                '> [!NOTE]',
                '> No schema changes detected. The production database already matches `schema.sql`.',
              );
            } else {
              const lines = diff.split('\n');
              const adds = lines.filter(l => l.startsWith('+')).length;
              const drops = lines.filter(l => l.startsWith('-')).length;
              const alters = lines.filter(l => /^(ALTER|MODIFY)/i.test(l.trim())).length;

              const stats = [
                adds > 0 ? `**${adds}** addition${adds > 1 ? 's' : ''}` : '',
                drops > 0 ? `**${drops}** removal${drops > 1 ? 's' : ''}` : '',
                alters > 0 ? `**${alters}** alteration${alters > 1 ? 's' : ''}` : '',
              ].filter(Boolean).join(', ');

              sections.push(
                '### Declarative Schema',
                '',
                '> [!IMPORTANT]',
                '> The following SQL will be applied to the **production** database on merge.',
                '',
                stats ? `${stats}\n` : '',
                '```sql',
                diff,
                '```',
              );
            }

            sections.push('', '### River Migrations', '');

            if (riverStatus === 'error') {
              sections.push(
                '> [!CAUTION]',
                '> Failed to check River migration status.',
                '',
                '<details>',
                '<summary>Error output</summary>',
                '',
                '```',
                riverOutput,
                '```',
                '',
                '</details>',
              );
            } else if (riverStatus === 'pending') {
              sections.push(
                '> [!IMPORTANT]',
                '> Pending River migrations will be applied on merge.',
                '',
                '```',
                riverOutput,
                '```',
              );
            } else {
              sections.push(
                '> [!NOTE]',
                '> All River migrations are up to date.',
              );
            }

            const body = [
              marker,
              '## Migration Plan — Production',
              '',
              ...sections,
              '',
              `<sub>Generated by Atlas \u00b7 ${new Date().toISOString().slice(0, 10)}</sub>`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c => c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
