# Automatically set AWS profile when entering this directory
# Install direnv: brew install direnv
# Add to ~/.zshrc: eval "$(direnv hook zsh)"
# Then run: direnv allow

export AWS_PROFILE=astro

echo "✓ AWS_PROFILE set to: astro"
